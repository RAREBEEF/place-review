import { ReactElement } from "react";
import styles from "./EditReview.module.scss";

const EditReview: React.FC = (): ReactElement => {
  return <div className={styles.container}></div>;
};

export default EditReview;
